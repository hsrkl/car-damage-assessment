[project]
name = "damage-assessment"
version = "0.1.0"
description = "Car damage assessment library using YOLO-based part detection and geometric reconstruction"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.2",
    "opencv-python>=4.13.0.92",
    "ultralytics>=8.4.14",
]
